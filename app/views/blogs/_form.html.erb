<%= form_for(@blog) do |f| %>

<!-- This 1st section controls errors -->
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!-- This 2nd section controls the partial form -->

  <div class="field" style="margin: 20px">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field" style="margin: 20px">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions" style="margin: 20px">
    <%= f.submit :class => "submit_button"%>
  </div>
<% end %>

<!-- This 3rd section controls the partial form -->




